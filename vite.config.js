// This file exists to help Hostinger detect the project as a Vite app.
// Astro uses Vite internally — the actual build is handled by astro.config.mjs.
import { defineConfig } from 'vite';
export default defineConfig({});
